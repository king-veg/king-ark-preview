const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('is-visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));const filters=[...document.querySelectorAll('.filters button')],cards=[...document.querySelectorAll('.style-track article')];filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter||'all';cards.forEach(c=>c.style.display=(f==='all'||c.dataset.cat===f)?'block':'none')}));const hero=document.querySelector('.hero-image');window.addEventListener('scroll',()=>{if(hero&&scrollY<innerHeight*1.2)hero.style.transform=`scale(${Math.max(1,1.05-scrollY*.00004)}) translateY(${scrollY*.05}px)`},{passive:true});